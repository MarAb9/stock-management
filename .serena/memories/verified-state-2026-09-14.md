# Verified state audit - 2026-09-14
- Scope: audit only, no code changes. Verified local `main` after `git fetch --prune`; `HEAD` equals `origin/main` at `ba5401ce5f11f34746536dba142d7529dc7b1cc9`.
- Stack confirmed: Laravel 13.17+/PHP ^8.3, Sanctum 4.3, PostgreSQL via Docker, DomPDF, Endroid QR; Angular 21, Angular Material/CDK 21, Tailwind 4.
- Backend DONE: session/Sanctum auth; products/reference data; immutable stock movements; stock balances; lot tracking with FEFO; inventory sessions/lines/validation adjustments; assets/equipment; assignment/transfer/return; maintenance; attachments; audit logs; institution settings; generic reports plus PDF/CSV/document/QR endpoints; Docker backend/database; backend suite passes.
- Backend PARTIAL: no role/policy/RBAC layer; PostgreSQL constraints/triggers exist but normal test suite uses SQLite so DB-level immutability/concurrency still needs PostgreSQL-specific verification; attachments/institution/audit/document endpoints are backend-only from current SPA.
- Frontend DONE: guarded Angular shell and routes for dashboard, products, stock movements, assets, inventory, reports, settings, authentication; pages call real API endpoints for core list/create/operate/report flows.
- Frontend PARTIAL: stock movement form is aligned with backend conditional validation, lot handling and FEFO behavior. Products/assets/settings expose create/basic listing, not full edit/archive/delete/detail flows. Inventory UI handles create/open/count/validate, but not add-line/cancel. Reports preview/export generic report types, but not dedicated document/label endpoints.
- MISSING end-to-end UI: attachments, audit log browsing, institution settings/logo, product lots/balances detail, product edit/archive, asset edit/archive/history endpoint, inventory add-line/cancel, QR labels and movement/inventory/asset PDFs.
- UX/UI PARTIAL: the application now has accessible global loading/error/success feedback, guarded writes, reusable confirmations and loaded-aware empty states. Tables/forms remain dense inline component templates pending the later visual-design phase.
- Tests verified 2026-09-15: `docker compose exec -T backend php artisan test` previously passed 20 tests/187 assertions; `cmd /c npm test -- --watch=false` passed 7 frontend files/24 tests; `cmd /c npm run build` passed. Angular test/build require the elevated path on Windows because sandboxed esbuild hits `spawn EPERM`; both Angular commands show the existing Sass `@import` deprecation warning.
- Repo/production PARTIAL: no `.github` CI directory; Docker runtime is present for backend/database only; migrations/seeding are explicit; deployment hardening, production env review, frontend hosting/deployment path, E2E tests, and PostgreSQL-specific constraint/concurrency checks remain missing/needs verification.
- Suggested next dependency order: finish remaining non-stock form alignment; add missing institution/audit/attachment/document/label UI only where product needs it; add RBAC if more than one staff role is expected; add CI with backend suite/frontend test/build; add PostgreSQL-specific integrity checks; polish responsive/a11y/design system; add focused E2E workflows; production env/deployment review.
