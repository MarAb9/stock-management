import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatInputModule } from '@angular/material/input'; import { MatSelectModule } from '@angular/material/select';
import { ApiService } from '../core/api.service';
@Component({standalone:true,imports:[FormsModule,MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<section class="heading"><h1>Référentiels</h1><p>Catégories, unités, fournisseurs et emplacements.</p></section><mat-card><div class="bar"><mat-form-field><mat-label>Type</mat-label><mat-select [(ngModel)]="type" (selectionChange)="load()"><mat-option value="categories">Catégories</mat-option><mat-option value="units">Unités</mat-option><mat-option value="locations">Emplacements</mat-option><mat-option value="suppliers">Fournisseurs</mat-option></mat-select></mat-form-field><mat-form-field><mat-label>Nom</mat-label><input matInput [(ngModel)]="name"></mat-form-field>@if(type!=='suppliers'){<mat-form-field><mat-label>Code</mat-label><input matInput [(ngModel)]="code"></mat-form-field>}<button mat-flat-button (click)="save()">Ajouter</button></div><table><thead><tr><th>Nom</th><th>Code / Société</th><th>Actif</th></tr></thead><tbody>@for(x of rows();track x.id){<tr><td>{{x.name}}</td><td>{{x.code||x.company||'—'}}</td><td>{{x.active?'Oui':'Non'}}</td></tr>}</tbody></table></mat-card>`,styles:`h1{margin:0;font-size:28px}p{color:#687278}.heading{margin-bottom:18px}mat-card{padding:18px}.bar{display:flex;gap:10px;align-items:center;flex-wrap:wrap}.bar button{background:#123a4a;color:white}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:11px 8px;border-bottom:1px solid #e6e8e9}`})
export class SettingsComponent {private api=inject(ApiService);readonly rows=signal<any[]>([]);type='categories';name='';code='';constructor(){this.load();}load(){this.api.get<any>('settings/'+this.type).subscribe(x=>this.rows.set(x.data));}save(){if(!this.name)return;const body:any={name:this.name,active:true};if(this.type!=='suppliers')body.code=this.code||this.name.toUpperCase().replace(/\s+/g,'-');this.api.post('settings/'+this.type,body).subscribe(()=>{this.name='';this.code='';this.load();});}}
